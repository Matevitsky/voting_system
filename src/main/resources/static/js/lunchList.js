/*var lunchTable = $('#lunchTable').DataTable({

    ajax: {
        url: 'api/lunches',
        dataSrc: '_embedded'
    },
    columns: [


        {
            data: 'lunches[0].name'
        },

        {
            data: 'lunches[0]._links[0].menu'

        },


    ]
});


$.getJSON("api/lunches", function (json) {
    console.log(json);

});
$.getJSON("api/lunches", function (json) {

$.('#lunchTable').DataTable({
    dataSrc: json


})
});*/
